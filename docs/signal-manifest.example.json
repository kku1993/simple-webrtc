{
  "version": 1,
  "shards": [
    { "name": "t", "url": "wss://t.signal.example/v1/signal", "weight": 3 },
    { "name": "k", "url": "wss://k.signal.example/v1/signal", "weight": 1 },
    { "name": "m", "url": "wss://m.signal.example/v1/signal", "weight": 0 }
  ],
  "settings": {
    "note": "Anything under `settings` is passed through untouched and readable via PeerConnection.currentManifest. ICE/TURN servers are not carried here — the server mints short-lived TURN credentials and returns them in the handshake responses; see docs/DESIGN.md §\"CreateRoom\"."
  }
}
